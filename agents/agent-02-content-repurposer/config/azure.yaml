# Azure overlay for Agent 02 - interface-complete stub.

provider: azure

llm:
  provider: azure
  azure_region: eastus
  tier_models:
    cheap: azure/gpt-4o-mini
    strong: azure/gpt-4o

cost:
  is_mock: false
  provider_currency: USD
  fx_rates:
    USD: 83.0
  input_cost_per_token_inr:
    cheap: 0.0
    strong: 0.0
  output_cost_per_token_inr:
    cheap: 0.0
    strong: 0.0
  ceiling_inr: 30.0

object_storage:
  provider: azure
  bucket: REPLACE_ME_AT_GO_LIVE
  prefix: content-repurposer/v1/

output_storage:
  enabled: true
  prefix: content-repurposer/v1/

secret_store:
  provider: env

telemetry:
  provider: stdout
