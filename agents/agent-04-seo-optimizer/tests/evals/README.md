# Agent 04 Eval Stubs

These are v1 eval stubs for the SEO Optimization Agent. They define the cases
and metrics that should become a CI-gated eval harness once Agent 04 is hardened
against live and larger offline datasets.

Pass policy:

- overall score >= 80
- no hard-fail risks
- all required fields present
- schema validation passes
