# infra/azure — STUB

Terraform for Azure AI / Azure OpenAI deployment. **Placeholder directory in v1** — Terraform stubs are filled when Azure
goes live. The agent reaches Azure only through `core` interfaces selected by `azure.yaml`;
going live is "fill the stub + flip config," not a redesign (DESIGN §4.2).
