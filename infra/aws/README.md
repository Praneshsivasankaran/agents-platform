# infra/aws — STUB

Terraform for AWS Bedrock deployment. **Placeholder directory in v1** — Terraform stubs are filled when AWS goes live.
The agent reaches AWS only through `core` interfaces selected by `bedrock.yaml`; going
live is "fill the stub + flip config," not a redesign (DESIGN §4.2).
