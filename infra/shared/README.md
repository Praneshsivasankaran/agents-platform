# infra/shared

Shared infrastructure referenced by all agents: registries, networking, and the
observability backend (e.g. an OpenTelemetry collector that `Telemetry` exports to).

Stub in v1 — Terraform modules land during Debug/Harden (DoD parity).
